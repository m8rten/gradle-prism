buildscript {
    dependencies {
        classpath files("../../../build/libs/plugin-HEAD-SNAPSHOT.jar")
    }
}

apply plugin: 'prism'

spy {
    url 'http://localhost:8080/api/superinvocation/'
    filter 'Jenkins'
}
